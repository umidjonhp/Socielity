import React from 'react'

export const Button = ({ children }) => {
  return (
    <button className="rounded-lg bg-blue-default px-[34px] py-3 text-[#fff] text-base font-bold">{children}</button>
  )
}
